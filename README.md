press **`** to open the dev console
# feature tags (POSSIBLY POORLY MAINTAINED)
- `tutorial` makes you always load into the tutorial for testing
- `skip_popups` skips popups
- `editor_start_main_menu` does that (usually, running the game from the editor skips the menus and loads you straight into the game)
